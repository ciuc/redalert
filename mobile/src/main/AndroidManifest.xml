<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.antiprotv.redalert">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_redalert"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_redalert_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AlertListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_about"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".HelpActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_help"
            android:screenOrientation="portrait"></activity>
        <receiver
            android:name="ro.antiprotv.redalert.AlertListActivity$MyBroadcastReceiver"
            android:exported="false" >
        </receiver>
    </application>

</manifest>